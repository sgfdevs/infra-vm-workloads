apiVersion: v1
kind: Secret
metadata:
  name: seaweedfs-s3-admin
  namespace: seaweedfs
  labels:
    app.kubernetes.io/part-of: seaweedfs
type: Opaque
stringData:
  access_key: "{{ bootstrap_argocd_seaweedfs_admin_access_key }}"
  secret_key: "{{ bootstrap_argocd_seaweedfs_admin_secret_key }}"
