---
- name: Apply apps root
  ansible.builtin.include_tasks: apply-manifest.yml
  vars:
    bootstrap_argocd_manifest_name: apps root
    bootstrap_argocd_manifest: "{{ lookup('file', role_path ~ '/files/argocd-apps-root.yaml') }}"
