---
- name: Bootstrap K3s cluster prerequisites
  hosts: k3s_cluster
  become: true
  gather_facts: true
  tasks:
    - name: Placeholder task
      ansible.builtin.debug:
        msg: "K3s bootstrap playbook scaffold is in place."
